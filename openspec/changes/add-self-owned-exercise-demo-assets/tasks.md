## 1. [基础设施] 静态资产边界与版本控制

- [x] 1.1 建立 `art/exercise-static` 目录、生产 manifest/schema、动作 brief/schema、审核记录和 staging 忽略规则，不恢复 Blender/GIF 目录
- [x] 1.2 重新核对并归档实施时 imagegen 商业使用条款，明确原创文字、自有母版和受限第三方媒体的输入边界
- [x] 1.3 建立以 `BuiltinExercise.code` 为主键的来源台账，记录 prompt/brief、工具/模型版本、输入、权利状态、人工修改、审核和 SHA-256
- [x] 1.4 实测角色/风格母版与高分辨率动作母版大小，只有超过普通 Git 预算时才配置路径限定 Git LFS，并验证远端可用
- [x] 1.5 编写跨电脑恢复说明，确保 fresh clone 可取得已批准生产资源且不包含 API key、缓存、候选图或失败图

## 2. [美术] 统一角色与风格母版

- [x] 2.1 使用原创文字 brief 生成 `character-v1` 正面、45°、侧面角色表，锁定脸型、短深色发型、体型比例、四肢、黑色训练短裤和灰阶解剖表现
- [x] 2.2 由项目所有者单独审核并批准 `character-v1`；记录角色身份清单、审核证据、时间和文件摘要
- [x] 2.3 依据此前批准的 `PEC_DECK_FLY` 效果图 1 建立 `style-v1`，锁定朱砂红主动肌、浅红协同肌、暖白背景、线稿、阴影、45° 构图、留白和无文字/Logo
- [x] 2.4 建立角色一致性联系表模板与拒绝规则，覆盖脸型、发型、体型、肢体比例、服装、手脚、左右方向和线稿风格

## 3. [美术] `PEC_DECK_FLY` 静态首个闸门

- [x] 3.1 编写 `PEC_DECK_FLY` 原创动作/器械 brief，明确顶部横梁式常规蝴蝶机、座椅、靠背、顶部转轴、弯曲摆臂、垂直握把、坐姿、握法和胸大肌高亮
- [x] 3.2 同时使用 `character-v1`、`style-v1` 与动作 brief 作为 reference/edit 输入生成高分辨率静态候选，不使用第三方图片
- [x] 3.3 对候选分别完成人物一致性、美术、动作、器械和权利审核；任一失败项重新生成且不进入正式目录
- [x] 3.4 将项目所有者最终批准的唯一母版、prompt/brief、输入清单、审核记录和 SHA-256 纳入版本控制
- [x] 3.5 在真实双列紧凑图卡片中导出并验收 288×288、JPEG 质量 82、≤24 KiB JPG，锁定白底安全边距、3x 清晰度和单文件预算

## 4. [基础设施] 静态导出与严格校验

- [x] 4.1 实现从已批准高分辨率母版确定性裁切、烘焙暖色背景归一、纯白背景合成、sRGB 转换和 288×288、JPEG 质量 82 JPG 导出脚本
- [x] 4.2 实现 promote 闸门，只有 `identity/art/movement/equipment/rights` 全部人工批准且技术校验通过才更新运行时 manifest
- [x] 4.3 实现严格校验：未知/重复 code、缺文件、288×288 尺寸、JPEG 质量 82、≤24 KiB、颜色、解码、摘要、孤立图片、候选误入库和动态/Blender 文件回流
- [x] 4.4 生成角色联系表和 288×288/双列卡片审核页，让项目所有者可并排比较全部已批准动作

## 5. [美术] 全量预置动作自治生产

- [x] 5.1 在 `PEC_DECK_FLY` 首道闸门通过后，为其余 11 个代表动作逐一编写原创动作/器械 brief
- [x] 5.2 以同一 `character-v1` 和 `style-v1` 按 `preset_exercises_v1.json` 覆盖全部内置动作；169 个动作生成唯一高分辨率母版和正式 JPG，10 个争议动作保留在跳过台账
- [x] 5.3 每个动作分别完成自治的人物一致性、美术、动作、器械、权利和技术审核；无法得出唯一标准或不合格项已写入跳过台账，不以占位凑齐
- [x] 5.4 已将 169 个正式 JPG 汇总为全量联系表，并按动作族完成角色、比例、服装和渲染风格抽样核对

## 6. [iOS] 动作库静态缩略图接入

- [x] 6.1 新增只读 `ExerciseArtworkLibrary` 和运行时 manifest 解析，按 `BuiltinExercise.code` 返回随包 288×288 JPG，不新增第三方依赖
- [x] 6.2 将动作区调整为双列 `LazyVGrid` 小尺寸图卡片，单卡限制为 88–104pt、媒体区约 1.25:1，标题区使用最低 26pt 高度与 12pt 字号，并使用白底、上疏下密的等比缩放和分级安全边距；移除肌群 PNG 的烘焙背景，按“纯白底正式动作图 → 透明底 `MuscleMapThumbnail` → 首字”三级降级，browse/search/pick/自定义占位共用同一卡片规则
- [x] 6.3 保持自定义动作、动作详情 `MuscleMapView`、筛选、删除与训练数据行为不变，并将装饰图片对 VoiceOver 隐藏、整卡合并朗读
- [x] 6.4 添加 manifest 映射、缺文件/损坏、未知 code、自定义同名、搜索和 pick 场景测试
- [x] 6.5 在 iPhone 17 Pro Simulator 真路径抽样验收了双列卡片、3x 清晰度、等宽布局、快速滚动、按 code 搜索、动作卡片进入详情和正式图/肌群图降级路径

## 7. [基础设施 / 后端边界] 收口验证

- [x] 7.1 测量全量运行时资源总大小、App 包体增量、动作库首次加载、快速滚动和峰值内存，评估全量发布边界
- [x] 7.2 已运行静态资产严格校验、相关 iOS 单元测试和 iPhone 17 Pro Simulator Debug 构建
- [x] 7.3 在 fresh clone 中拉取所需 LFS 对象、重导出正式 JPG、比对 manifest/摘要，并验证不安装 AI 工具也可构建 iOS
- [x] 7.4 已检查最终 diff 不含 Blender/GIF/动态播放代码，也未修改后端、数据库、同步协议、训练记录或 Team 数据
- [x] 7.5 严格校验 OpenSpec；只有全部已批准或已记录跳过的预置动作、真实列表抽样和恢复验证全部通过后才允许归档
