## 1. iOS 端实现

- [x] 1.1 增加计划动作静态组次摘要与详情展示数据派生，覆盖普通动作、次数不一、递减组和超级组
- [x] 1.2 将计划详情动作行改为默认折叠、全页单一展开，并保持原生 `List` 滚动与稳定 `itemId`
- [x] 1.3 将动作卡右侧改为包含编辑/删除的 `…` 菜单，移除独立编辑按钮和左滑删除，保留删除二次确认
- [x] 1.4 实现普通动作、递减组、超级组展开详情，展示完整组次、备选动作、自适应下次重量和来源
- [x] 1.5 更新计划详情用户可见说明文案，统一使用“训练安排”“组次详情”“下次训练重量”

## 2. 后端与基础设施

- [x] 2.1 确认本 change 不需要后端、数据库、同步 payload、依赖或部署配置改动

## 3. 测试与验证

- [x] 3.1 增加静态组次摘要和详情数据派生测试，覆盖普通动作、次数不一、递减组、超级组与自适应重量
- [x] 3.2 运行 iOS 测试并通过
- [x] 3.3 运行仓库约定的无签名 iPhone 17 Pro Simulator build 并通过
- [x] 3.4 严格校验 `refine-plan-exercise-card-details` OpenSpec change

## 4. UI 与交互优化

- [x] 4.1 将动作卡 `…` 菜单入口改为无背景、无描边、无阴影的纯图标，同时保留 34pt 点击区域
- [x] 4.2 将展开详情拆为独立原生 `List` 行，移除布局动画、交叉淡化、顶部位移与 spring 回弹
- [x] 4.3 使用 Simulator、Computer Use 与录屏逐帧复查菜单视觉、单一展开和动作切换效果
- [x] 4.4 重新运行 iOS build、相关测试与 OpenSpec strict validation
