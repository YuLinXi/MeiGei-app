## 1. iOS 端领域逻辑

- [x] 1.1 调整 `WorkoutExercise.toggleWarmup(_:)`，基于 `isWarmupEffective` 切换并归一旧 raw 值，且不再修改 `setIndex`。
- [x] 1.2 更新相关代码注释，使展示分段、稳定原序和调用方保存职责与新规则一致。

## 2. iOS 端回归测试

- [x] 2.1 增加原生热身取消、正式组双向切换可逆及 `setIndex` 全程不变的领域测试。
- [x] 2.2 增加取消热身不污染最后正式组预填来源、旧 `setTypeRaw == "warmup"` 一次切回正式组的兼容测试。

## 3. 后端 / 基础设施与验证

- [x] 3.1 确认后端、数据库、同步 DTO 与基础设施无需修改，并通过 OpenSpec strict 校验。
- [x] 3.2 运行相关 iOS 测试与无签名模拟器构建，确认现有排序、休息策略和编译均无回归。
