## 1. iOS 历史预填索引

- [x] 1.1 扩展现有历史投影，按普通组、递减组、超级组成员和超级组配对建立最近 completed 实绩索引。
- [x] 1.2 提供普通组、递减组和超级组表单可复用的同结构预填查询，并保持计划预填接口语义不变。

## 2. iOS 训练创建交互

- [x] 2.1 修复训练页排序入口，按一级训练单元数量显示并保持现有单元级排序提交逻辑。
- [x] 2.2 将普通动作与递减组临时创建接入同结构历史预填和既有无历史默认值。
- [x] 2.3 为训练中超级组创建表单接入配对优先、成员回退的预填，并保护用户手动输入。
- [x] 2.4 在训练录入页出现时请求历史索引加载，且不改变计划详情中的超级组表单语义。

## 3. iOS 验证

- [x] 3.1 添加历史索引与三类预填的单元测试，覆盖结构隔离、别名、热身/未完成过滤、配对反向匹配和无历史回退。
- [x] 3.2 运行相关 DontLiftTests、OpenSpec 严格校验与无签名 Simulator build；在 Simulator 回归添加和排序路径。
