## 1. iOS 计划模型与开始训练

- [x] 1.1 为 `PlanItem` 增加 optional `restAfterSetSeconds`，保持普通动作/递减组三态语义，并添加新旧 JSON round-trip 兼容测试。
- [x] 1.2 将个人计划和 Team 分享计划的普通动作/递减组默认休息映射到新建 `WorkoutUnit`，保持超级组现有轮后休息映射与休息继承优先级不变。
- [x] 1.3 添加计划开始训练测试，覆盖显式秒数、关闭、跟随全局、严格/自适应模式和旧计划缺字段。

## 2. iOS 自适应回写与撤销

- [x] 2.1 在 `PlanWriteback` 按一级训练单元合并普通动作、递减组和来源超级组的默认休息，并确保临时新增动作携带休息、跳过项保持不变、临时超级组不自动写回结构。
- [x] 2.2 明确只使用训练单元默认休息作为回写来源，添加测试证明 `plannedRestSeconds`、`actualRestSeconds` 和单次倒计时调整不会覆盖计划默认值。
- [x] 2.3 将休息纳入回写变化判定和摘要，验证 rest-only 更新会展示回执，撤销后恢复原值并重新标脏；严格计划不产生回写。

## 3. iOS 计划与训练 UI

- [x] 3.1 在普通动作/递减组计划编辑中复用现有休息选项，支持跟随全局、关闭、常用秒数与自定义正整数，并保存到计划项。
- [x] 3.2 在计划详情的训练安排中展示“跟随全局”“关闭自动休息”或显式秒数，保持折叠卡和计划列表不增加冗余信息。
- [x] 3.3 为训练中动作休息菜单增加恢复“跟随全局”的入口并区分当前解析值，补充输入边界与 accessibility 验证。

## 4. iOS 计划转换与 Team 路径

- [x] 4.1 更新无计划训练保存模板和个人计划复制路径，保留普通动作、递减组与超级组休息，同时维持既有重量清理规则。
- [x] 4.2 更新客户端 Team 无重量快照重建逻辑以保留休息，验证分享版本直接开始和 Fork 后均传递休息且保持独立。
- [x] 4.3 添加转换路径测试，覆盖显式秒数、关闭、跟随全局、重量剥离及普通动作/递减组/超级组三类单元。

## 5. 后端与基础设施边界

- [x] 5.1 增加或更新后端 Team 计划分享测试，证明递归剥离重量时保留休息键，分享版本与 Fork 的 items JSON 不丢失该字段。
- [x] 5.2 确认计划同步继续透明保存 items JSON，并记录本 change 无 Flyway、API、部署配置或新依赖变更。

## 6. 验证与验收

- [x] 6.1 运行计划预填、自适应回写、休息策略、模板转换与 Team 分享相关 iOS 定向测试。
- [x] 6.2 使用 iPhone 17 Pro Simulator 运行无签名 Debug build，并运行相关后端定向测试。
- [x] 6.3 在 Simulator 人工验证自适应回写/撤销、严格不回写、跟随全局、关闭、显式秒数及普通动作/递减组/超级组下次带入路径。
- [x] 6.4 运行 `openspec validate carry-plan-rest-defaults --strict` 与 `git diff --check`，确认本 change 改动聚焦，并保留工作区中用户已有的训练名称同步改动；跨旧客户端编辑仍存在丢失未知休息字段的兼容风险。
