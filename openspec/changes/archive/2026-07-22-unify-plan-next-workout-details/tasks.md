## 1. iOS 端展示派生

- [x] 1.1 在现有 `PlanItemDisplay`/相邻纯展示层增加下次安排与静态模板的语义比较，覆盖组顺序、组类型、热身、重量、次数和递减组 segments，并忽略 UUID 等非展示身份
- [x] 1.2 增加诚实的模板基准摘要派生：统一值可紧凑展示，逐组值不同或递减结构复杂时不得选取虚假代表组
- [x] 1.3 增加动作语义感知的重量/组值格式化，明确自重显示“自重”、其他空重量显示“训练时填写”，且不修改持久化值

## 2. iOS 端计划详情

- [x] 2.1 重构普通动作和递减组展开详情，只遍历 `PlanPrescriptionPreview.sets` 派生的一份完整安排，并在同一行组合重量与次数
- [x] 2.2 在自适应详情标题附近紧凑展示 preview 来源；仅在模板与下次安排有语义差异时显示一行“模板基准”，移除重复的静态组次列表和“下次训练重量”列表
- [x] 2.3 将严格模式详情收敛为一份按计划复制的“训练安排”，保留“完成后不更新”说明和既有缺失值校验
- [x] 2.4 调整超级组详情为轮数、成员动作完整重量 × 次数、轮后休息的统一结构，并确保展示值与 `PlanWorkoutBuilder` 实际使用字段一致
- [x] 2.5 保留备选动作、默认折叠、单一展开、原生 `List`、稳定行身份、动作菜单和无显式布局动画，并让长数值与辅助功能字号不截断关键训练数据

## 3. 后端与基础设施

- [x] 3.1 确认本 change 不修改后端 API、数据库、Flyway、同步 payload、依赖、部署配置或自适应历史优先规则

## 4. 测试与验证

- [x] 4.1 增加自适应普通动作测试：模板 5 组而历史 3 组时，展示组数、重量与次数和 `PlanWorkoutBuilder` 生成结果完全一致
- [x] 4.2 增加热身组、严格模式和模板差异比较测试，覆盖相同基准隐藏、不同基准显示以及复杂设置不生成虚假摘要
- [x] 4.3 增加递减组测试，逐一校验逻辑组、segments 顺序、重量与次数均和实际预填一致
- [x] 4.4 增加超级组与空重量文案测试，覆盖成员值、轮数、轮后休息、自重和“训练时填写”
- [x] 4.5 运行 iOS 单元测试、UI 测试和仓库约定的无签名 Simulator build
- [x] 4.6 使用 Simulator 人工回归普通组、热身组、递减组、超级组、严格/自适应、模板差异和辅助功能字号，并确认展开切换无重叠或布局跳动
- [x] 4.7 严格校验 `unify-plan-next-workout-details` OpenSpec change，并运行 `git diff --check`
