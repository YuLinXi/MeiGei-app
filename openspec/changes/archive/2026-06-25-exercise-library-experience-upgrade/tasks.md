> 依赖：本 change 须在 `exercise-library-taxonomy-import` 归档后再实施（同改 `ExerciseViews.swift` / `ExerciseCategory.swift` / `MuscleRegion.swift` / 内置数据文件）；与 `muscle-map-detailed-art` 协同。详见 design「依赖与排序」。
> 安全前提：被删/改 code 全属未发布的导入 928 条 → 干净删改、无需历史合并（design D6）。

## 1. 分类核心：解剖三级树（iOS 端，模型）

- [x] 1.1 `Models/ExerciseCategory.swift`：L1 由 15 收缩到 11——解剖 8（胸/背/肩/手臂/腿/臀/核心/颈部）+ 非解剖 3（有氧/功能性/热身拉伸）
- [x] 1.2 新增 L2「肌肉」层与 L1→L2 归属（胸→胸大肌；背→背阔肌/斜方肌/菱形肌/竖脊肌；肩→三角肌；手臂→肱二头肌/肱三头肌/前臂；腿→股四头肌/腘绳肌/内收肌/小腿；臀→臀大肌/臀中肌；核心→腹直肌/腹斜肌；颈部→颈部肌）
- [x] 1.3 L3「肌头/肌区」按需配（design D1）：胸大肌→上胸/中下胸；三角肌→前束/中束/后束；腹直肌→上腹/下腹；其余 L2 的 L3 留空（不强配）
- [x] 1.4 `Models/MuscleRegion.swift`：补缺失区 `deltSide`（肩中束）、`rhomboids`（中背/菱形）等，使高亮叶级与树对齐；标注哪些区在 L2、哪些在 L3
- [x] 1.5 `Models/BuiltinExercise.swift`：`category` 语义=L1；`subcategory` 重锚为 L3 肌头（可空、按需）；L2 归属由 `primaryRegions` 派生（不新增显式字段）
- [x] 1.6 单测：L1 共 11、rawValue 无重复；L1→L2→L3 树各级无重复且归属合法；`MuscleRegion` 全集与树对齐、每个 region 归属唯一 L1

## 2. 重映射现有动作到三级树（iOS 端，数据，体力活）

- [x] 2.1 现有 153 curated 重新归位 L1/L2/L3（**code 不变**，仅改归属）；被旧 15 类降级者（二头/三头/前臂→手臂，小腿→腿，斜方肌→背）正确落新 L1 的 L2
- [x] 2.2 928 导入条按 L1 分批重映射 L1（+ 已有 region/subcategory 者落 L2/L3）；未回填 region 者落「L1→全部」（数据债，不阻塞）
- [x] 2.3 单测：每条 `category` 命中 11 个 L1；非空 `subcategory` 均属其 L2 的合法 L3；curated code 全部不变

## 3. 发布前数据去噪（iOS 端，数据）

- [x] 3.1 删除间歇·Tabata 全部 22 条（`BuiltinExercise+Imported.swift`），产出删除清单
- [x] 3.2 版本号去重·有本体（9 组）：留本体删 `_V2/_V3`（−10）
- [x] 3.3 版本号去重·孤儿（9 条）：名称去尾缀 + code 去 `_V2/_V3` 后缀（转正不删）
- [x] 3.4 难度/负重变体合并（13 族）：留本体删（中难度/高难度/负重）变体（−18）
- [x] 3.5 功能性补 5 子类（爆发奥举/抗旋稳定/髋臀激活/负重搬运/动态控制，design D7），102 条逐条归类（可分批，未归类落「全部」）
- [x] 3.6 去噪自检：全库 `code` 唯一；无任何「（版本」字样；有氧不再含「间歇·Tabata」；总量 1081 → ~1031（与清单核对）

## 4. 动作库版式·路 B 三级左栏（iOS 端）

- [x] 4.1 `Workout/ExerciseViews.swift`：部位轴改固定窄左栏竖排（全部/我的/11 L1 + 数量角标），自身可纵滚
- [x] 4.2 左栏**逐级手风琴**：L1 展开 L2、L2 展开 L3，点哪级过滤到哪级；无下级不展开；**删除独立子分类横滚 chip 行**
- [x] 4.3 器械轴保留右侧顶部 `HorizontalChipPicker`，与解剖树正交叠加
- [x] 4.4 **删除 `LibraryGroup`**；右侧分段按树层级（选全部→L1 段、选 L1→L2 段、选 L2→L3 段、无下级则平铺；design D4）
- [x] 4.5 自定义动作「我的」入口 + 选部位时按 `primaryMuscle` 并进对应段
- [x] 4.6 同段排序：curated 优先（design D8）
- [x] 4.7 空态/降级保留：库空占位卡；高亮按「最深可染级 + 优雅回退」（D2），缺区不崩
- [x] 4.8 展示层折叠单一中间肌肉层：胸/肩左栏与右侧分段直接显示 L3，内部仍保留完整 L2/L3

## 5. 搜索·中文模糊（iOS 端）

- [x] 5.1 `matchesQuery` 改多关键词 AND（空格拆词，每词都需被名称 `localizedCaseInsensitiveContains`，顺序无关）
- [x] 5.2 搜索全局通道：有 query 时忽略左栏 L1/L2/L3、全库匹配、结果按 L1 分段、左栏选中态淡化；器械 chip 仍叠加（design D5）
- [x] 5.3 清空 query 回到左栏当前选择浏览态；占位「搜索 {N} 个动作」N 同步去噪后合计
- [x] 5.4 单测：「杠铃 卧推」与「卧推 杠铃」均命中「平板杠铃卧推」；空格容错；纯中文子串旧行为不回归

## 6. 验收

- [x] 6.1 `xcodebuild build` + 测试 target 编译通过；训练记录/休息计时/PR/计划引用不回归
- [ ] 6.2 三级树可用：左栏 L1→L2→L3 逐级下钻收窄正确；右侧按对应层级分段；器械正交叠加；自定义并段
- [ ] 6.3 分组修复核对：选「胸」右侧直接按 L3（上胸/中下胸）分段、选「肩」直接按 L3（前束/中束/后束）分段；多 L2 部位仍按 L2 分段；不再出现「单关节」伪桶
- [ ] 6.4 高亮回退核对：上/下胸/中束等美术未拆区，高亮回退父级或暂隐、不崩
- [x] 6.5 现有 153 historyKey/曲线不断裂；去噪未触碰任何已发布老 code；归位未改 code
- [ ] 6.6 搜索多词 AND 命中正确；搜索态左栏让位、器械仍叠加

## 7. 高亮美术补绘（与 muscle-map-detailed-art 协同，分批，不阻塞上线）

- [ ] 7.1 补 `deltSide`（肩中束）、`rhomboids`（中背）等新区的纸感矢量美术
- [ ] 7.2 L3 paintable 区（上/下胸、上/下腹、三角三束等）按需分批补绘，补一批接一批的回退降级
- [ ] 7.3 每批补绘后抽查高亮渲染：新区上色正确、未补区仍优雅回退
