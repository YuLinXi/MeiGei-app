## 1. iOS 端：估算核心与本地偏好

- [x] 1.1 新增可测试的 `WorkoutCalorieEstimator`，按时长、估算体重、粗粒度强度计算整数 kcal 与展示文案。
- [x] 1.2 新增本地 `WorkoutCaloriePreferences`，用 `UserDefaults` 保存估算体重与显示开关，不接入同步和后端。

## 2. iOS 端：页面展示

- [x] 2.1 在「我的 > 训练偏好」加入估算体重设置与消耗估算开关。
- [x] 2.2 在已完成训练详情页三联数下方展示 kcal 估算行，并在缺少体重或关闭开关时隐藏。
- [x] 2.3 在训练分享海报数据与画布中重点展示 `约 xxx kcal`，并在缺少体重或关闭开关时隐藏。
- [x] 2.4 确认训练进行中、首页列表、Team 打卡和 HealthKit 写入不展示或写入 kcal。

## 3. iOS 端：自动化测试与验证

- [x] 3.1 增加估算算法测试，覆盖常规/高强度/低强度、无体重、无有效时长。
- [x] 3.2 增加海报数据测试，覆盖 kcal 可用和隐藏两种路径。
- [x] 3.3 运行 OpenSpec 校验、iOS 自动化测试和 iOS 编译验证。

## 4. 后端 / 基础设施

- [x] 4.1 确认后端、数据库、同步 DTO、HealthKit active energy 和基础设施无变更。
