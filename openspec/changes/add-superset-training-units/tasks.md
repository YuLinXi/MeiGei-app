## 1. iOS 数据模型与兼容

- [x] 1.1 为训练记录新增一级训练单元模型，支持单动作与超级组两类，并保留旧 `Workout.exercises` 的兼容读取。
- [x] 1.2 为计划新增超级组计划单元字段，兼容旧 `WorkoutPlan.items` 单动作结构。
- [x] 1.3 增加超级组成员、轮数、轮后休息与按轮完成状态的领域辅助方法。

## 2. iOS 训练与计划流程

- [x] 2.1 在计划详情/编辑路径支持创建超级组：选择两个动作、统一组数、分别设置重量和次数，并复用自研数字键盘输入规则。
- [x] 2.2 在进行中训练支持新增超级组，并生成两个成员动作的普通工作组。
- [x] 2.3 在训练页按轮展示超级组，主完成粒度为「完成本轮」，并支持轮后休息设置。
- [x] 2.4 支持超级组增减轮数与解除超级组，且不允许热身组、递减组或嵌套超级组。

## 3. iOS 统计、同步与回写

- [x] 3.1 更新训练统计、PR、历史与周统计路径，确保超级组每完成一轮计为两个普通工作组。
- [x] 3.2 更新从计划开始训练与自适应回写逻辑，只回写成员动作的重量/次数，不改变超级组结构和轮数。
- [x] 3.3 更新计划同步与 Team 计划分享的 JSON 编解码，保留旧 payload 兼容。

## 4. iOS 展示面

- [x] 4.1 更新训练详情、计划卡片与计划详情摘要，展示超级组成员、轮数和总组数。
- [x] 4.2 更新 Team 打卡详情摘要，保留超级组结构展示而不是扁平化成普通动作。
- [x] 4.3 更新训练分享海报摘要，展示「超级组 · A + B · N 轮」。

## 5. 验证

- [x] 5.1 运行 OpenSpec 校验，确认提案、设计、规格与任务可应用。
- [x] 5.2 运行 iOS 相关编译验证。
