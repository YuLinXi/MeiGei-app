## 1. 数据与 manifest

- [x] 1.1 基于 `docs/exercise-library-preset-v1.md` 生成结构化预置动作 manifest，字段至少包含 `code`、标准名、器械、部位、细分、主动肌区、协同肌区、别名、legacy code 和历史保留状态
- [x] 1.2 生成别名兼容 manifest，覆盖旧 code、旧名称、线上自定义名称到标准动作的映射
- [x] 1.3 生成移除/历史保留清单，明确计时型有氧、HealthKit 泛称、版本动作、左右侧动作、难度/负重变体和已确认移除动作的处理策略
- [x] 1.4 增加 manifest 校验：标准 `code` 唯一、标准名唯一性合理、别名不得指向不存在的标准动作、移除动作不得出现在可新建预置动作中
- [x] 1.5 增加线上历史覆盖校验脚本或本地校验命令，输出训练历史遗漏集合和计划引用遗漏集合
- [x] 1.6 运行生产库只读覆盖校验，确认遗漏集合为 0，且输出不包含用户身份、训练日期、重量、次数或备注

## 2. iOS 端

- [x] 2.1 调整 `BuiltinExercise` 或动作数据加载层，读取新的预置动作 manifest、别名兼容 manifest 和移除/历史保留清单
- [x] 2.2 实现动作解析器：标准 code 优先，其次 legacy code，其次别名/旧名称，最后历史快照降级展示
- [x] 2.3 调整动作选择页为单一预置动作库展示，移除任何“核心库/扩展库/扩展动作”用户可见概念
- [x] 2.4 调整默认筛选为部位、器械和搜索；胸部保留上胸/中下胸，肩部保留前束/中束/后束
- [x] 2.5 调整搜索逻辑，使标准名、别名、legacy code 对应旧名称和线上自定义兼容名称均可命中标准动作
- [x] 2.6 确认合并动作搜索只返回标准动作，例如「绳索十字夹胸」返回「绳索夹胸」，「哑铃轮换弯举」返回「哑铃交替弯举」
- [x] 2.7 确认独立动作不会被误合并，例如「直杆绳索弯举」与「绳索弯举」、「悍马机卧推」与「悍马机推胸」、「器械倒蹬机」与「腿举」
- [x] 2.8 确认历史记录、PR、训练计划详情引用旧 code 或旧名称时仍能展示标准名、器械、部位和可用肌肉高亮
- [x] 2.9 确认已明确移除但有历史保留策略的动作不出现在新建训练动作选择结果中，历史详情仍可展示

## 3. 后端

- [x] 3.1 确认本 change 不新增后端 API、数据库表、同步实体或 Flyway 迁移
- [x] 3.2 确认 Team 计划、训练同步和历史数据仍只依赖稳定动作 code / 快照名称，不依赖动作库是否默认可见
- [x] 3.3 仅保留生产库只读 SQL 或脚本用于覆盖校验，不向后端服务增加运行时逻辑

## 4. 基础设施与验证

- [x] 4.1 运行 OpenSpec 校验，确保 `exercise-library` delta spec 可归档
- [x] 4.2 运行 iOS 编译验证：`xcodebuild -project DontLift.xcodeproj -scheme DontLift -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -configuration Debug CODE_SIGNING_ALLOWED=NO build`
- [x] 4.3 手动验证动作选择页：部位筛选、器械筛选、胸/肩细分、标准名搜索、别名搜索、移除动作搜索
- [x] 4.4 手动验证历史与计划：`LAT_PULLDOWN`、`PEC_DECK_FLY`、`CABLE_CROSSOVER`、`DB_ROTATING_CURL`、`FACE_PULL`、`MACHINE_ROW` 等旧引用均能展示标准动作
- [x] 4.5 记录回滚方式：恢复上一版随包动作 manifest 和加载逻辑，应能恢复旧动作库展示；由于不改后端 schema 和同步模型，回滚只涉及 iOS 包内资源
