## 1. iOS 端

- [x] 1.1 提取肌群图共享渲染辅助，避免详情页与缩略图重复维护 `MuscleRegion` 到 `MuscleMap.Muscle` 的映射。
- [x] 1.2 新增紧凑肌群缩略图组件，支持单面展示、主肌群高亮、协同肌弱化、自动选择正/背面和稳定尺寸。
- [x] 1.3 将动作库/添加动作抽屉的内置动作行左侧头像位接入肌群缩略图；`primaryRegions` 为空时保留首字头像。
- [x] 1.4 保持自定义动作行、动作详情页完整肌群图、搜索/筛选/PR 展示行为不变。
- [x] 1.5 将列表缩略图收敛为有限解剖大类图，并使用生成后的缓存图片展示，避免每个动作行实时绘制 SDK view。
- [x] 1.6 在不伪造 SDK 不支持细分的前提下，将缩略图扩展为有限可区分分组；支持可视觉区分的主动肌区域，无法区分的子分类保持大类图。
- [x] 1.7 将有限分组缩略图离线导出为 `xcassets` PNG，并将列表运行时切换为纯 asset 读取，移除列表缩略图的 `ImageRenderer` / `NSCache` 生成逻辑。

## 2. 后端 / 基础设施

- [x] 2.1 确认本 change 不需要后端、数据库、同步协议、APNs 或基础设施改动。

## 3. 验证

- [x] 3.1 运行 OpenSpec 校验，确认 proposal/design/spec/tasks 可解析。
- [x] 3.2 运行 iOS Debug simulator build，确认 SwiftUI 代码编译通过。
