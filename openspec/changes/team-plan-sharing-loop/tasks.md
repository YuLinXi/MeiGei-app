## 1. 后端数据模型与迁移

- [x] 1.1 [后端] 新增 Flyway 迁移：创建 `team_plan_share`、`team_plan_share_version`、`team_plan_share_event` 表，使用 UUID 主键、`app_user.id` 用户引用、软删除、必要索引与唯一约束。
- [x] 1.2 [后端] 在迁移中将既有 `workout_plan.shared_to_team_id IS NOT NULL` 的非删除计划回填为 v1 分享版本，并在快照中去除重量字段。
- [x] 1.3 [后端] 新增 Team 计划分享实体、DTO、Mapper，并实现按 Team 查询最新分享版本与聚合统计的 SQL。
- [x] 1.4 [后端] 实现分享版本 items 清洗：保留动作引用、名称快照、顺序、组数、次数、稳定 itemId，去除所有作者重量字段。
- [x] 1.5 [后端] 为分享、Fork、反馈事件写接口接入既有幂等键机制，避免弱网重复创建版本、计划或事件。

## 2. 后端服务与 API

- [x] 2.1 [后端] 实现 `POST /teams/{teamId}/plan-shares`：校验当前用户拥有计划且是 Team 成员，创建或追加无重量分享版本。
- [x] 2.2 [后端] 实现 `GET /teams/{teamId}/plan-shares`：返回分享计划卡所需的最新版本、作者、动作预览、复制人数与总完成次数。
- [x] 2.3 [后端] 实现 `POST /teams/plan-share-versions/{versionId}/fork`：从分享版本创建当前用户的独立私有 `WorkoutPlan`，不复制作者重量和分组。
- [x] 2.4 [后端] 实现 `POST /teams/plan-share-versions/{versionId}/events`：记录 `fork`、`direct_start`、`complete` 最小化反馈事件，不创建 Team checkin。
- [x] 2.5 [后端] 保留旧 Team 计划接口兼容窗口：旧列表读取新模型，旧 Fork 转调最新分享版本 Fork，避免旧 TestFlight 客户端立刻失效。
- [x] 2.6 [后端] 补充测试：非成员拒绝、分享版本去重量、再次分享生成新版本、Fork 独立、反馈统计去重、complete 不创建 checkin。

## 3. iOS 数据模型与网络层

- [x] 3.1 [iOS] 扩展 API 模型：新增 Team 分享计划卡、分享版本、分享事件请求/响应 DTO，支持复制人数和总完成次数。
- [x] 3.2 [iOS] 扩展 `TeamService`：新增分享到 Team、读取分享计划、从版本 Fork、记录反馈事件接口。
- [x] 3.3 [iOS] 为 `WorkoutPlan` / `Workout` 增加分享版本软关联字段或等价本地来源模型，并同步 DTO 中需要跨设备保留的字段。
- [x] 3.4 [iOS] 新增 pending Team 计划反馈事件队列，完成训练离线或弱网时可重试发送 `complete` 事件。
- [x] 3.5 [iOS] 更新 Team 分享计划版本到 `PlanItem` / `WorkoutExercise` 的转换逻辑，确保无法识别动作时使用名称快照兜底或阻止开始。

## 4. iOS 首页与完成训练闭环

- [x] 4.1 [iOS] 修改训练首页 CTA：移除 active plan 自动选择逻辑，首页始终创建 `planId == nil` 的无计划训练。
- [x] 4.2 [iOS] 保留个人计划详情页明确“开始训练”入口，确保从个人计划开始仍按既有预填和自适应回写规则运行。
- [x] 4.3 [iOS] 在完成训练页新增“保存为计划模板”行动卡，仅对无个人计划来源训练默认展示。
- [x] 4.4 [iOS] 实现保存为计划模板 sheet：名称、模式、分组选择，并从已完成正式组生成个人 `WorkoutPlan`。
- [x] 4.5 [iOS] 确保从 Team 分享计划直接开始的训练完成后不触发个人计划回写，但可保存为新的个人计划。
- [x] 4.6 [iOS] 新增自动同步期间的全局非阻塞顶部进度提示。
- [x] 4.7 [iOS] 将进行中训练记录页的返回行为改为最小化，并确保左滑返回后显示全局训练中悬浮窗。
- [x] 4.8 [iOS] 为自动同步提示增加 1-2 秒最短展示，连续同步时不闪烁。
- [x] 4.9 [iOS] 将进行中训练改为全局浮层/悬浮窗呈现，任意页面可展开训练记录且收起后保留当前路由。
- [x] 4.10 [iOS] 将训练首页、个人计划详情、Team 计划页的进行中训练入口迁移到全局 presenter，不再 push `WorkoutLoggingView`。
- [x] 4.11 [iOS] 设计并接入专用训练最小化按钮，替代普通返回/下箭头样式。
- [x] 4.12 [iOS] 首页底部 CTA 文案固定为「开始训练」。
- [x] 4.13 [iOS] 统一训练组完成态下重量、次数、完成按钮背景色与动画，并仅为完成按钮点击增加触感反馈。
- [x] 4.14 [iOS] 保存为计划弹窗默认大高度展示，并将分组选择改为项目内纸感卡片单选 UI。
- [x] 4.15 [iOS] 补充单元测试或轻量逻辑测试：首页 CTA 无计划、训练生成计划项、个人计划训练不重复展示保存入口。

## 5. iOS Team 分享计划体验

- [x] 5.1 [iOS] 在个人计划详情页新增「分享到 Team」入口和 Team 选择/确认说明，明确分享版本不包含作者重量。
- [x] 5.2 [iOS] 改造 `TeamPlansView` 为 Team 分享计划页：展示计划名、上次更新时间、分享者、动作预览、复制人数、总完成次数，且不展示版本号或计划模式。
- [x] 5.3 [iOS] 在 Team 分享计划卡提供主操作「开始训练」：基于当前分享版本创建训练，记录 `direct_start` 反馈事件。
- [x] 5.4 [iOS] 在 Team 分享计划卡提供次操作「Fork 到我的计划」：调用新 Fork 接口，同步后提示已保存到「计划」。
- [x] 5.5 [iOS] 在首次直接开始或页面说明中展示隐私边界：完成可计入聚合次数，但不会公开训练详情。
- [x] 5.6 [iOS] 处理空状态、加载失败、不可开始分享版本、重复点击、弱网重试等 UI 状态。

## 6. 验证与回归

- [x] 6.1 [后端] 运行 `./gradlew test`，覆盖 Team 分享计划、版本、反馈统计和旧接口兼容。
- [x] 6.2 [iOS] 运行 simulator build，确认新增模型、SwiftData 迁移和 Team/Workout UI 编译通过。
- [x] 6.3 [iOS] 手工验证首页 CTA：有最近计划时仍创建无计划训练，计划详情页仍可明确从计划开始。
- [x] 6.4 [iOS] 手工验证完成页：无计划训练可保存为计划，个人计划训练不重复提示，Team 分享计划直接开始后可保存为计划。
- [ ] 6.5 [端到端] 验证 Team 闭环：分享到 Team → Team 计划页展示 → 直接开始 → 完成计入聚合但不出现在 Feed → 按次分享后出现在 Feed。
- [ ] 6.6 [端到端] 验证 Fork 闭环：分享到 Team → Fork 到我的计划 → 作者再次分享新版本 → 旧 Fork 不更新 → 再 Fork 新版本生成新计划。
- [x] 6.7 [OpenSpec] 运行 `openspec validate --all`，确认 proposal/design/spec/tasks 合法。

## 7. 本轮缺陷修复

- [x] 7.1 [后端/iOS] Team 计划支持作者删除自己的分享计划，并拒绝删除他人分享计划。
- [x] 7.2 [后端/iOS] 修复作者编辑原计划后再次分享到 Team 被幂等键回放旧版本的问题，确保 Team 列表展示最新分享快照。
- [x] 7.3 [iOS] 优化 Team 计划列表：按“我分享的计划 / 成员分享计划”分组，卡片只展示上次更新时间、分享者、动作预览、统计与采用操作。
- [x] 7.4 [iOS] 增加 Team 分享计划详情页，点击卡片可查看完整动作组数/次数，且不展示作者重量、版本号或计划模式。
- [x] 7.5 [iOS] 修复计划分组删除反馈，确认按钮文案改为“删除”并展示删除成功反馈。

## 8. Review 后补修复

- [x] 8.1 [后端] 修复账号删除转移 Team owner 时误删该 Team 内其他成员分享计划和反馈事件的问题，只清理注销用户自己的分享数据。
- [x] 8.2 [后端] Team 分享计划相关写接口强制要求 `Idempotency-Key`，并复用全局幂等过滤器回放响应。
- [x] 8.3 [后端] 反馈事件增加 `workout_id` 维度去重插入，避免 `complete` 弱网重试重复计数；`direct_start` 支持训练软指针。
- [x] 8.4 [后端] 分享版本创建改为锁定同一 Team/作者/来源计划的 share 线索，并处理并发插入冲突。
- [x] 8.5 [后端] 分享 items 去重量改为解析失败拒绝、递归移除重量字段，避免异常时原样保存隐私字段。
- [x] 8.6 [iOS/后端] 新版分享请求携带当前本地无重量计划快照，不再依赖分享前 `syncAll()` 作为版本屏障。
- [x] 8.7 [iOS] Team 计划反馈事件改为本地 pending 队列优先写入，发送成功后再移除，并为直接开始训练携带 workoutId。
- [x] 8.8 [iOS] 补齐不可开始分享版本的禁用态、加载态、隐私说明文案和旧“发布”用户可见文案清理。
- [x] 8.9 [iOS] 保存为计划模板入口只在存在已完成正式组时展示，生成计划项时避免 orderIndex 空洞。
